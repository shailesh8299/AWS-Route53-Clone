# Frontend environment variables for Route53 Clone
# Copy this file to .env.local and adjust as needed

# Base URL of the FastAPI backend server (no trailing slash, no /api suffix)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
